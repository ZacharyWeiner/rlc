<%= form_with(model: kill_switch, local: true) do |form| %>
  <% if kill_switch.errors.any? %>
    <div id="error_explanation">
      <h2><%= pluralize(kill_switch.errors.count, "error") %> prohibited this kill_switch from being saved:</h2>

      <ul>
      <% kill_switch.errors.full_messages.each do |message| %>
        <li><%= message %></li>
      <% end %>
      </ul>
    </div>
  <% end %>

  <div class="field">
    <%= form.label :is_active %>
    <%= form.check_box :is_active, id: :kill_switch_is_active %>
  </div>

  <div class="actions">
    <%= form.submit %>
  </div>
<% end %>
